SELECT id, "Name"
	FROM public.card_holder;

SELECT card, cardholder_id
	FROM public.credit_card;
	
SELECT id, name
	FROM public.merchant_category;
	
SELECT id, name, id_merchant_category
	FROM public.merchants;